<resources>
    <!-- Base application theme using modern Material3. -->
    <style name="Theme.MaterialGuardian" parent="android:Theme.Material.Light.NoActionBar">
        <!-- We use Jetpack Compose for the UI, so these are just fallback values -->
        <item name="android:colorPrimary">#6750A4</item>
        <item name="android:colorSecondary">#625b71</item>
    </style>

    <style name="Theme.MaterialGuardian.Launch" parent="Theme.MaterialGuardian">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources>
